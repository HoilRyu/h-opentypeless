> 2026-09-08 결정 변경: 최초 커서/앱 자동 복원은 폐기했다. 포커스가 다른 앱에 있으면 복사 창을 띄운다. 아래 자동 복원 관련 계획은 과거 계획이며 현재 범위가 아니다. 현재 상태는 docs/fork/HANDOFF.md 참조.

# H-OpenTypeless 포크 개발 계획

작성일: 2026-09-08
작업 경로: `/Users/ryuhoil/syncthing/workspace/h-opentypeless`
상태: GitHub 포크 생성·로컬 클론·upstream 연결 완료. 독립 앱 구성·STT/Ollama 직접 연결을 구현하고 Mac 빌드 및 사용자 실제 음성 입력 확인 완료. 상세 진행은 docs/fork/HANDOFF.md 참고. Windows/Linux 실제 검증은 마지막에 진행한다.

## 1. 목표와 범위

OpenTypeless의 기존 음성인식, 교정, 제공자 설정, 프롬프트, 기록, 단축키 기능을 최대한 그대로 사용한다. 공식 업데이트를 가져오기 쉽게 변경 지점을 작게 유지하면서 다음 기능을 추가한다.

1. 기존 Android 음성 키보드를 Mac 앱에 연결한다.
2. Mac 설정 창을 닫으면 Dock에서는 사라지고 상단 메뉴 막대에만 남는다.
3. Mac에서 녹음을 시작할 때의 입력 대상을 기억하여, 다른 창이나 입력칸으로 이동하더라도 처리가 끝나면 처음 위치로 돌아가 입력한다.

macOS·Windows·Linux를 모두 제품 지원 대상으로 한다. 현재 Mac에서는 로컬 실행을 검증하고 Windows·Linux는 해당 운영체제 CI 빌드·테스트와 실제 환경의 실행 검증을 별도로 수행한다. Android IME는 기존 구현을 참고하여 필요한 소스를 H 저장소로 선별 이관한 뒤 이곳에서 독립 관리한다. 공통 모바일 API·제공자·설정 기능은 세 운영체제에서 동작하도록 구현한다. Dock 정책은 macOS 전용이며 Windows/Linux의 트레이 동작은 각 플랫폼에 맞게 유지한다. 최초 입력 위치 복원은 macOS Accessibility, Windows UI Automation, Linux X11/Wayland의 지원 범위를 각각 검증하고 플랫폼별 어댑터로 분리한다. 한 플랫폼에서만 검증한 기능을 전체 지원으로 표시하지 않는다.

포크는 기존 순정 앱과 구분되는 개발 앱으로 시작한다. 최초 검증을 통과하기 전까지 현재 설치된 앱·서비스·Android 설치본을 교체하거나 삭제하지 않는다.

## 2. 현재 구성과 인계

참고 전용 프로젝트: `/Users/ryuhoil/syncthing/workspace/mobile-opentypeless`

### 개발 경계 — 2026-09-08 사용자 결정

- 실제 코드·문서·테스트·배포 작업은 모두 `h-opentypeless`에서 진행한다.
- 기존 모바일 프로젝트는 동작, UI, API 계약, 실패 사례를 읽어보는 참고 자료다. 그곳을 수정하거나 두 프로젝트를 병행 개발하지 않는다.
- H 소스에서 기존 프로젝트를 import하거나 심볼릭 링크로 연결하지 않는다. 기존 Python 게이트웨이·추론 프록시를 H의 실행 또는 빌드 필수 조건으로 삼지 않는다.
- Android의 필요한 구현만 H의 `android/`로 한 번 선별 이관하고, 이후 변경과 빌드는 H를 기준으로 한다. 기존 설정·서명키·캐시·APK·녹음·venv를 통째로 복사하지 않는다.
- Qwen ASR와 Ollama는 외부 추론 엔진으로 사용할 수 있다. 이 외부 엔진 의존성과 기존 모바일 프로젝트 코드 의존성을 구분한다. 연결·실행 요구사항은 H 문서에 기록한다.
- 현재 사용 중인 순정 앱 및 기존 서비스는 그대로 둔다. H 개발을 이유로 자동 중지·삭제·설정 변경하지 않는다. 실제 교체는 검증 후 별도 전환 작업으로 한다.

| 구성 | 현재 동작 | 포크에서의 방향 |
|---|---|---|
| 공식 OpenTypeless | 설치 앱, 사용자 지정 STT/LLM 주소 사용 | 공식 소스 기반 별도 개발 빌드 |
| Android 키보드 | 네이티브 Java IME, 결과 확인 후 삽입, 좌우/Backspace 반복 | 필요한 소스를 H로 선별 이관, H에서 독립 개발 |
| Python 모바일 게이트웨이 | 내부 IP:8787, 오디오 변환 → STT → LLM → JSON 반환 | 앱 내부 모바일 연결 모듈로 단계적 대체 |
| Qwen3-ASR | Mac MLX 서버, loopback:8765 | 우선 기존 외부 엔진 유지 |
| Ollama/Gemma4 12B | loopback:11434 | 기존 외부 엔진 유지 |
| 추론 해제 프록시 | loopback:11435, reasoning_effort=none만 추가, 프롬프트/응답 보존 | H는 Ollama 직접 호출 및 제공자 전용 추론 옵션으로 대체 |
| Custom Polish Instructions | 공식 앱 설정에서 관리 | 포크에서도 앱 설정이 원본 |

과거 `polish_server.py`의 자체 프롬프트 및 숫자·부정 검사 구조는 폐기했다. 이를 되살리지 않는다. 현재 `backend/ollama_proxy.py`는 프롬프트를 담당하지 않는다.

Android v0.4.1은 실행 충돌 수정 및 실행 유지 확인을 마쳤지만, 키보드가 열린 상태의 설정 화면 가려짐 시각 검증은 남아 있다. 이 검증을 포크 작업 완료로 착각하지 않는다.

사용자는 연결키 입력을 제거했고 내부 Wi-Fi/WireGuard IP·HTTP 사용을 선택했다. 이 결정을 유지한다. 모바일 연결은 사용자가 앱에서 켜는 옵션으로 만들고, 선택한 사설 인터페이스에만 바인딩한다. HTTPS·인증키 재도입은 이번 범위에 포함하지 않는다.

## 3. 원본 업데이트를 따라가는 저장소 운영

### 기본 원칙

- 공식 upstream의 폴더 구조, 이름, 포맷과 기존 기능을 유지한다.
- 전체 코드 포맷 변경, 디렉터리 재배치, 불필요한 의존성 업데이트를 기능 작업과 섞지 않는다.
- 추가 기능은 독립 모듈에 두고, 기존 코드에는 호출·설정·수명 관리 연결만 추가한다.
- 플러그인 시스템을 새로 만드는 것처럼 큰 추상화는 피한다. 실제 필요한 작은 경계부터 만든다.
- 공식 구현과 동일한 처리 코드를 복사해 두 벌로 관리하지 않는다.

### 확정한 Git 구성과 개발 운영안

- `origin`: `https://github.com/HoilRyu/h-opentypeless.git` — 생성 완료한 공개 포크.
- `upstream`: 공식 저장소 `https://github.com/tover0314-w/opentypeless`.
- 기본 작업 브랜치는 포크 통합용으로 사용하고, 기능별 단기 브랜치를 나눈다.
- 시작 기준은 공식 `v1.1.57`이다. 로컬 `main`, `origin/main`, `upstream/main`, `v1.1.57^{commit}`이 모두 `68cf6f11ad6632b86217a1729e33bb8d560610cf`임을 확인했다. 구현 시작 시 원격 변경을 다시 확인하되 기준 버전을 자동 변경하지 않는다.
- 공식 main 전체를 무조건 따라가지 않고 릴리스 단위로 통합한다.
- 통합된 공유 브랜치를 반복 rebase하지 않는다. 업데이트 전용 브랜치에서 upstream 릴리스를 merge하고 검증한 뒤 반영한다. 아직 공유하지 않은 기능 브랜치의 rebase는 가능하다.
- 기능별 커밋: 배포 식별자 분리 / 창 수명 / 입력 대상 / 모바일 API / 선택적 Ollama 옵션 등으로 분리한다.
- `docs/fork/UPSTREAM.md`에 기반 태그·커밋, 공식 파일 수정 목록, 수정 이유, 충돌 해결 사항과 검증 결과를 기록한다.
- 공식에 같은 기능이 들어오면 자체 패치를 제거하고 공식 기능으로 전환한다.

### 업데이트 절차

1. 현재 동작 버전에 태그를 붙이고 앱 설정/DB를 백업한다.
2. 공식 릴리스 및 설정/DB 마이그레이션 변경을 확인한다.
3. 업데이트용 브랜치에서 upstream 변경을 합친다.
4. 아래 변경 경계 목록을 우선 검토한다.
5. upstream 검사와 포크 회귀 검사를 실행한다.
6. 기존 포크 설정을 가진 상태에서 업그레이드하여 실행·단축키·입력·모바일 연결을 확인한다.
7. 문제가 있으면 이전 앱뿐 아니라 필요한 설정/DB 백업도 복구한다. DB 마이그레이션의 역호환을 가정하지 않는다.

## 4. 제안 구조와 변경 경계

아래는 구현 예정 경로이며 아직 생성하지 않았다. 실제 기반 릴리스 구조에 맞춰 최소 조정한다.

```text
h-opentypeless/
  FORK_PLAN.md
  src-tauri/src/extensions/
    mod.rs                 # 앱 초기화·종료에 연결
    mobile/                # HTTP 요청, 제한, 오디오 입력, JSON 응답
    mac_window/            # 창 표시 상태와 Dock 정책
    settings.rs            # 포크 전용 설정
  src/components/ForkSettings/  # 추가 기능 설정 화면
  android/                 # 참고 구현에서 선별 이관한 H Android 앱 및 빌드 지침
  docs/fork/
    HANDOFF.md             # 완료 사항·검증 결과·바로 다음 작업
    UPSTREAM.md
    MOBILE_API.md
    COMPATIBILITY.md
    RELEASE.md
```

| 공식 코드 변경 경계 | 최소 변경 목적 |
|---|---|
| `src-tauri/src/lib.rs` 및 기존 tray/창 열기 함수 | 확장 초기화·종료, main 창 show/hide 정책 연결 |
| `pipeline.rs` | 최초 대상 캡처/최종 삽입 연결, 오디오 처리 공통 진입점 |
| `app_detector`·`output` | 기존 기능을 활용하는 대상 복원 어댑터 |
| 설정 UI 진입점 | 별도 확장 설정 패널 연결 |
| 빌드·Tauri 설정 | 포크 앱 ID·이름·배포 경로 분리 |
| `llm/protocol.rs` | Ollama 전용 추론 옵션을 추가하는 작은 패치 |

포크 전용 설정은 별도 네임스페이스/파일로 저장하고 버전을 둔다. 공식 설정 타입에 많은 필드를 추가하지 않는다. 기존 설정으로 충분한 항목은 재사용한다. 이를 위해 별도 설정 시스템 전체를 재구현하지 않는다.

## 5. 기능 A — Android 연결 통합

### 목표 흐름

```text
Android 키보드
 → 포크 앱의 모바일 API
 → 기존 STT 제공자
 → 기존 프롬프트 생성 + 기존 LLM 제공자
 → Android에 원문/교정문 반환
 → 사용자가 휴대폰 입력칸에 삽입
```

모바일 요청은 Mac의 현재 입력창에 타이핑하지 않는다. Mac 전용 입력 대상 복원 기능도 실행하지 않는다.

### 구현 방향

- 앱 안에 Rust HTTP 모듈을 추가하고 기존 pipeline에서 오디오→텍스트 결과를 얻는 작은 공통 함수를 추출한다. 오디오 처리와 데스크톱 출력 동작을 분리하며 전체 pipeline 재작성은 하지 않는다.
- 모바일과 데스크톱이 공식 프롬프트 생성 및 제공자 구현을 공유한다. 프롬프트를 HTTP 모듈에 복제하거나 숨은 지침을 추가하지 않는다.
- 모바일에는 Mac의 활성 앱·선택 텍스트가 없으므로 일반 받아쓰기 문맥을 사용한다. 사용자 지정 지침은 공유하되 데스크톱의 앱별 문맥까지 같다고 가정하지 않는다.
- 현재 Android 오디오 형식 AAC/M4A를 지원한다. 기존 공식 디코더 지원 여부를 먼저 확인하고, 부족하면 mobile 모듈 경계에 디코딩을 추가한다. FFmpeg가 필요하면 초기 외부 의존성과 배포 번들을 명확히 구분한다.
- 녹음 파일은 임시 저장 후 삭제하고, 앱 종료·오류·클라이언트 취소 시에도 정리한다.
- Mac 녹음과 모바일 요청의 동시 처리 정책을 명확히 한다. 초기에는 단일 처리 및 바쁨 응답으로 단순화하며, UI 녹음 상태와 모바일 작업 상태를 무리하게 혼용하지 않는다.

### API 호환성

현재 Android 앱을 먼저 수정하지 않아도 연결 가능하도록 유지한다.

- `GET /api/health`
- `POST /api/dictate`: multipart `file`
- 응답: `raw_text`, `polished_text`, `model`, `asr_ms`, `polish_ms`, `warning`
- 기존 제한: 20 MiB, 120초, 동시 처리 초과429, 원인별 오류 응답.
- `/download/android.apk`는 데스크톱 제품의 필수 실행 API가 아니다. 초기 배포는 APK 파일을 릴리스와 함께 제공하고, 기존 다운로드 경로가 필요한 경우 별도로 호환 여부를 결정한다.
- 확장 필드가 필요하면 기존 필드를 유지한 채 추가한다. API 버전은 health 응답에 추가할 수 있다.

### 앱 수명과 전환

- 설정에서 모바일 연결 켜기/끄기, 실제 IP·포트, 현재 연결 상태를 보여준다.
- 창 닫기에는 서버를 유지하고, 명시적 앱 종료에는 요청 정리 후 서버를 종료한다.
- 기존 게이트웨이와 포트 충돌 시 임의로 종료하지 않고 원인을 표시한다. 개발 검증에는 별도 포트를 쓰고, 통과 후8787로 이전한다.
- 검증 순서: 기존 Android 계약 테스트 → 직접 API 호출 → 실기기 녹음/교정/삽입 → WireGuard 연결.
- H의 API·Android 빌드는 기존 프로젝트나 Python 게이트웨이 없이 동작해야 한다. 기존 서비스의 중지·삭제는 이번 개발과 분리하며, 실제 전환 요청 시 진행한다. Qwen ASR와 Ollama는 별도 실행 엔진으로 남는다. 이들을 앱이 자동 관리하는 것은 별도 후속 범위다.

## 6. 기능 B — 창을 닫으면 메뉴 막대에만 유지

공식 코드에는 이미 `close_to_tray`, 메뉴 막대 아이콘, main 창 숨기기와 다시 열기 동작이 있다. 이를 재사용하고 macOS Dock 정책만 보완한다.

예상 상태:

| 동작 | main 창 | Dock | 메뉴 막대·단축키·모바일 서버 |
|---|---|---|---|
| 설정 창 열기 | 표시 | 표시 | 유지 |
| 창 닫기 / Command+W | 숨김 | 숨김 | 유지 |
| 메뉴 막대에서 설정·기록 열기 | 표시 | 표시 | 유지 |
| 작은 녹음 표시창만 표시 | main 숨김 | 숨김 유지 | 유지 |
| 명시적 종료 / Command+Q | 종료 | 제거 | 종료 |

- macOS ActivationPolicy의 Regular/Accessory 전환을 검토한다. UI 메인 스레드에서 수행하고 초기 창 생성 전에 접근하지 않는다.
- 모든 main 창 진입점(메뉴, 재실행, 단일 인스턴스 이벤트 등)을 작은 공통 함수로 연결한다.
- tray 생성이 실패한 경우 main 창과 Dock을 모두 숨겨 접근 불가능하게 만들지 않는다.
- 기존 close_to_tray 옵션과 상호 작용을 정의하고 이중 종료 처리를 피한다.
- macOS 이외 플랫폼에는 기존 정책을 유지한다.

## 7. 기능 C — 처음 입력칸과 커서 위치로 돌아가 입력

### 요구 동작

입력칸 A에 커서를 두고 녹음 시작 → 입력칸 B 또는 다른 앱으로 이동 → 음성인식·교정 완료 → A를 다시 활성화하고 처음 위치에 한 번만 삽입한다.

앱을 다시 활성화하는 것만으로 정확한 커서 위치 복원이 되지는 않는다. 현재 공개 코드의 TargetAppGuard는 프로세스/앱 식별을 중심으로 하므로 입력 요소와 선택 범위를 별도로 확인해야 한다.

### 먼저 실시할 기술 검증

- macOS Accessibility API로 시작 시점의 앱, 창, 편집 요소, 선택 범위를 얻을 수 있는지 확인한다.
- TextEdit의 일반 텍스트, 실제 사용하는 코딩 프롬프트 입력칸, 브라우저 textarea/contenteditable, Orca 터미널 순서로 검사한다.
- 터미널이나 일부 사용자 정의 편집기는 논리적 커서 위치를 외부에 제공하지 않을 수 있다. 키보드 화살표나 과거 화면 좌표로 위치를 추정해 입력하지 않는다.
- 호환성 결과를 '정확한 위치 복원 가능 / 같은 입력 요소만 복원 가능 / 복원 불가'로 기록한다. 마지막 두 경우를 정확한 커서 복원 성공으로 보고하지 않는다.

### 상태와 처리

1. **캡처:** 녹음 UI가 포커스를 가져가기 전에 최초 입력 대상 정보를 캡처한다. 접근성 요소 참조, 창 식별자, 선택 범위, 세션 번호를 메모리에 보관한다.
2. **처리:** 다른 창으로 이동해도 해당 녹음 세션의 처리 자체는 유지한다. 취소 또는 새 녹음에는 세션을 구분해 늦은 결과를 차단한다.
3. **검증:** 대상 앱/문서/요소가 살아 있고 편집 가능한지, 선택 범위를 복원할 수 있는지 확인한다. 비밀번호 입력칸은 제외한다.
4. **복원:** 원래 앱과 창·입력 요소를 활성화한 뒤 범위를 복원하고, 실제 포커스가 돌아온 것을 확인한다.
5. **삽입:** 공식 출력 모듈을 통해 한 번만 삽입한다. Enter나 명령 실행은 보내지 않는다.
6. **실패:** 임의의 현재 창에 넣지 않고 결과를 유지하여 복사/수동 삽입할 수 있게 한다. 실패 이유를 짧게 표시한다.

원래 문서가 변경되었다면 과거 문자 오프셋이 다른 위치를 가리킬 수 있다. 요소가 제공하는 변경 정보 또는 필요한 최소한의 주변 정보로 위치 유효성을 검사하고, 확인할 수 없으면 수동 삽입으로 전환한다. 이 정보는 로컬 세션 메모리에서만 사용하고 LLM이나 기록으로 보내지 않는다.

초기 적용 범위는 일반 받아쓰기 삽입이다. 선택 텍스트 편집·번역·Ask 흐름은 기존 의미가 달라 별도 검증 없이 동작을 바꾸지 않는다.

포크 설정에 '녹음 시작 위치로 돌아가 입력' 옵션을 두고, 끄면 공식 출력 경로가 그대로 작동하게 한다. 사용자 요청에 따라 완료 시 원래 앱으로 돌아오는 동작을 명확히 안내한다.

## 8. Ollama 직접 호출과 추론 해제

H는 Ollama의 OpenAI 호환 API를 직접 호출한다. 초기 제공자 검증 단계에서 Ollama에만 적용되는 추론 옵션을 추가하여 Gemma4를 그대로 사용하면서 `reasoning_effort: none`을 전달한다. 기존 Python 프록시11435는 H의 실행 경로에 넣지 않는다.

- 공식 프롬프트 생성과 Custom Polish Instructions를 그대로 사용한다. 별도 교정 프롬프트 서버는 만들지 않는다.
- 다른 API 제공자에는 해당 옵션을 넣지 않는다. 주소만 보고 모든 OpenAI 호환 서비스를 Ollama로 취급하지 않는다.
- 모델·메시지·일반 생성 옵션 및 응답 내용은 보존한다. 추론 옵션 외의 교정 규칙은 추가하지 않는다.
- 실제 Gemma4 응답, 추론 해제 여부, 공식 호출 제한 시간 안의 완료를 검증한다. 실패 시 기본 모델을 임의로 바꾸지 않고 원인을 해결한다.
- 현재 순정 앱이 사용하는 프록시는 별도 운영 상태로 남겨둔다. H 개발 중 기존 서비스 설정을 변경하지 않는다.

## 9. 실행 순서와 완료 기준

각 단계는 별도 기능 브랜치와 작은 커밋으로 진행한다. 단계별로 코드 변경 이유, 검증 결과, 남은 제한을 기록하고 검증한 변경만 포크 main에 통합한다. 아래 브랜치 이름은 제안이며 아직 생성하지 않았다.

| 단계 | 작업 / 브랜치 제안 | 완료 기준 |
|---|---|---|
| 0 | 독립 개발 기반 / `chore/fork-foundation` | v1.1.57 기준 기록, H 앱 ID·설정·업데이트 분리, 원본 기능을 유지한 개발 빌드 실행 |
| 1 | 외부 엔진 직접 연결 / `feat/ollama-reasoning-control` | H → Qwen ASR, H → Ollama 직접 호출로 음성인식·교정·삽입 성공; 기존 Python 코드 불필요 |
| 2 | 커서 복원 가능성 검증 / `spike/input-target` | 실제 사용하는 입력 앱별 정확한 위치·입력 요소 복원 지원 범위를 문서화 |
| 3 | Dock·메뉴 막대 / `feat/mac-menu-bar` | 닫기→Dock 숨김→메뉴로 복귀, 녹음·단축키 유지, 명시적 종료 성공 |
| 4 | 커서 복원 제품 구현 / `feat/input-target-restore` | 지원 앱에서 A→B→A 최초 위치에 1회 삽입; 대상 소멸·변경 시 결과 보존 |
| 5 | H 모바일 API / `feat/mobile-api` | 공식 STT·LLM·프롬프트 경로 공유, API 계약·오디오 변환·실패·동시 요청 검증 |
| 6 | H Android 앱 / `feat/android-client` | H 저장소만으로 빌드, 실기기 녹음·교정·삽입·편집 키·설정 화면 검증 |
| 7 | 배포와 업데이트 / `chore/fork-release` | H 앱·APK 산출물, 설치/연결/업데이트/되돌리기 문서, upstream 병합 절차 검증 |

2단계는 구현 비용이 큰 커서 복원의 가능성을 먼저 확인하는 작은 실험이다. 지원하지 않는 입력 앱의 대응 범위는 실험 결과로 정하며, 무조건 모든 앱에 입력되도록 우회하지 않는다.

### 첫 구현 작업의 구체적인 범위

1. 기반 SHA와 릴리스 태그를 `docs/fork/UPSTREAM.md`에 기록한다.
2. 공식 테스트·빌드 상태를 확인하고 기존 실패와 포크가 만든 실패를 구분한다.
3. 개발 표시 이름은 H-OpenTypeless, bundle identifier 후보는 `dev.hoilryu.hopentypeless`로 한다. 설정·Keychain·로그·URL scheme 등 공식 앱과 충돌할 식별자를 조사하여 필요한 부분만 분리한다.
4. 공식 자동 업데이트가 H 앱을 덮어쓰지 않도록 포크 배포 정책을 적용한다.
5. 의존성·빌드 출력은 동기화 외부 전용 경로에서 관리하고, H 소스만으로 재현 가능한 빌드 방법을 기록한다.
6. 첫 개발 빌드의 실행·설정 저장·종료를 확인하고, 변경 파일과 검증 결과를 `docs/fork/HANDOFF.md`에 남긴다.

이 단계에서 Android 연결이나 커서 복원까지 한꺼번에 추가하지 않는다. 독립 앱의 기본 실행이 첫 번째 검토 가능한 결과물이다.

### 단계 공통 검증

공식 CONTRIBUTING.md의 TypeScript 검사, ESLint, Prettier 검사, Vitest, Rust fmt/clippy/test를 기준으로 한다. 단순 문서 변경에는 빌드하지 않으며, 코드 변경의 해당 검사를 수행하고 통합·배포 시 전체 검사를 실행한다. 최초 기준점에서 이미 실패하는 검사는 기록하고 원인과 영향 없이 통과로 처리하지 않는다. 의존성 설치는 잠금 파일을 기준으로 하고 일괄 업그레이드를 섞지 않는다.

공식 업데이트가 새로 없으면 별도 검증 브랜치에서 업데이트 절차를 재현하고, 실제 미래 변경의 충돌까지 검증했다고 보고하지 않는다.

## 10. 검증 및 배포 원칙

- 공식 테스트·타입 검사·린트·Rust 검사와 포크의 실제 동작을 검증한다. 빌드 성공만으로 실행 확인을 대체하지 않는다.
- 입력 대상: 앱 전환, 같은 창의 다른 입력칸, 창/문서 닫힘, 편집 중 내용 변경, 취소, 연속 녹음, 잠금, 접근성 권한 거부, 비밀번호 필드.
- 창 수명: 메뉴 막대 재열기, Command+W/Q, 앱 재실행, 녹음 표시창, 모바일 처리 중 창 닫기/종료.
- 모바일: Wi-Fi/WireGuard, 모델 오류, 교정 끔, 긴/잘못된 파일, 동시 요청, 취소, 키보드 숨기기, 설정 화면에서 입력칸 노출.
- 포크 앱은 별도 bundle identifier·설정 저장소·Keychain/로그 경로를 사용한다. 공식 앱의 설정을 자동 공유하지 않고 필요할 때 명시적으로 복사한다.
- 초기 포크는 공식 자동 업데이트를 받지 않게 한다. 이후 포크 전용 릴리스 주소와 서명키를 사용한다. 공식 업데이트는 Git 통합을 통해 가져온다.
- 개발 앱과 공식 앱을 동시에 띄우면 전역 단축키·마이크·포트가 충돌할 수 있으므로 테스트 시 구분한다.
- Android는 기존 패키지·서명을 유지해야 업데이트 설치가 가능하다. 별도 테스트 패키지 여부를 정하고 서명키를 저장소에 넣지 않는다.
- Syncthing 폴더에는 소스·문서만 둔다. 모델, venv, target/build, 음성, 앱 데이터, 인증·서명키는 동기화 외부에 둔다. .gitignore만으로 Syncthing이 차단된다고 가정하지 않는다.

## 11. 구현 시작 전에 확정할 항목

- GitHub 공개 포크 `HoilRyu/h-opentypeless`는 확정했다. 앱 표시 이름·bundle identifier는 위 후보를 기준으로 구현 시 충돌 여부를 검증한다.
- 실제 주로 쓰는 Mac 입력 앱과 편집기 목록. 정확한 커서 복원의 우선 검증 대상.
- 기준 릴리스는 v1.1.57이며 현재 main과 동일함을 확인했다. 원격 업데이트가 생기더라도 기능 개발 도중 기준을 임의 변경하지 않는다.

그 외 세부 구현은 위 원칙에 맞춰 결정한다. 이번 요청은 계획서 작성만이며 위 작업들은 사용자가 구현 시작을 요청한 뒤 진행한다.

## 참고 자료와 확인 범위

공개 코드 확인일: 2026-09-08. 확인 시 공식 main 커밋: `68cf6f11ad6632b86217a1729e33bb8d560610cf`. 2026-09-08 로컬 확인으로 v1.1.57 태그도 동일 커밋임을 확인했으며 시작 기준으로 선정했다.

- [공식 저장소](https://github.com/tover0314-w/opentypeless)
- [창 닫기와 tray 구현](https://github.com/tover0314-w/opentypeless/blob/68cf6f11ad6632b86217a1729e33bb8d560610cf/src-tauri/src/lib.rs)
- [공식 pipeline](https://github.com/tover0314-w/opentypeless/blob/68cf6f11ad6632b86217a1729e33bb8d560610cf/src-tauri/src/pipeline.rs)
- [입력 대상 식별 타입](https://github.com/tover0314-w/opentypeless/blob/68cf6f11ad6632b86217a1729e33bb8d560610cf/src-tauri/src/app_detector/types.rs)
- [LLM API 요청 구성](https://github.com/tover0314-w/opentypeless/blob/68cf6f11ad6632b86217a1729e33bb8d560610cf/src-tauri/src/llm/protocol.rs)
- [공식 자동 업데이트 설정](https://github.com/tover0314-w/opentypeless/blob/68cf6f11ad6632b86217a1729e33bb8d560610cf/src-tauri/tauri.conf.json)
- [Apple 선택 텍스트 범위 속성](https://developer.apple.com/documentation/applicationservices/kaxselectedtextrangeattribute)

원본 코드에서 확인한 사실과 제안 설계를 구분한다. ActivationPolicy 전환과 앱별 Accessibility 선택 복원은 아직 구현·실기기 검증하지 않은 설계 후보이다.

## 2026-09-08 다중 플랫폼 검증 보완

- 원본 CI에는 Windows x64, macOS ARM64, Linux x64/ARM64 Rust 검사 구성이 있다. 구성 존재와 실제 통과 여부는 구분한다.
- 배포 전 각 운영체제에서 앱 실행·트레이 복귀·마이크·단축키·텍스트 삽입·모바일 API를 확인한다. Linux는 X11/Wayland를 구분한다.
- macOS 전용 API와 의존성은 조건부 컴파일 경계에 두며 공통 HTTP/STT/LLM 코드가 macOS 프레임워크에 의존하지 않게 한다.
- Windows/Linux에서도 최초 입력 대상 복원을 연구·구현하되 지원되지 않는 환경은 결과 보존·수동 삽입으로 처리하고 UI와 호환성 문서에 표시한다.
- 현재 Mac의 MLX 기반 ASR 서버가 Windows/Linux에서도 그대로 실행된다고 가정하지 않는다. H 앱은 제공자 API로 연결하며, Windows/Linux에서는 접근 가능한 ASR 서버 또는 해당 OS에서 지원되는 엔진을 설정한다. 로컬 STT 엔진의 동봉·자동 설치는 별도 범위다.
