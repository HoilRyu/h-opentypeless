H-OpenTypeless의 첫 GitHub 시험판입니다. OpenTypeless를 기반으로 개인 모델 연결과 모바일 음성 키보드를 추가했습니다.

## 다운로드

- macOS Apple Silicon: `H-OpenTypeless_0.1.43-beta.1_macos-arm64.dmg`
- Android arm64: `H-OpenTypeless_0.3.7_android-arm64.apk` (Android 9 이상)
- Windows, Linux, Intel Mac 바이너리는 실제 검증 후 추가합니다.

## 주요 기능

- 데스크톱 음성 입력과 AI 문장 교정, 직접 STT/LLM 제공자 연결
- 모바일 연결 메뉴와 내장 연결 서버: 별도 Python 게이트웨이 없이 Android 키보드 연결
- Android 로컬 Whisper/Gemma 모델 관리와 온디바이스 처리
- 입력창 근처 녹음 캡슐, 포커스 이탈 시 텍스트 복사 창, 설정 가능한 복사 단축키
- 녹음 중 음량 조절 및 복원, 녹음 취소·마이크 오류 처리 개선
- macOS 키체인 helper 재사용, H 전용 아이콘과 정리된 설정 화면

## 설치 및 업데이트

Mac DMG를 열고 H-OpenTypeless 아이콘을 오른쪽 Applications 폴더로 드래그합니다. 복사가 끝나면 응용 프로그램 폴더에서 앱을 실행하고 연결된 디스크 이미지를 추출합니다. 자체 서명된 시험판으로 Apple 공증은 받지 않았습니다. 처음 실행 시 차단되면 신뢰하는 다운로드인지 확인한 뒤 시스템 설정 → 개인정보 보호 및 보안에서 해당 앱의 실행을 허용합니다. 시스템 보안 기능 전체를 끌 필요는 없습니다. 마이크와 손쉬운 사용 권한, 최초 키체인 접근 허용이 필요할 수 있습니다.

Android는 APK를 직접 설치하고 음성 키보드 및 마이크 권한을 설정합니다. 기존 개발용 APK와 배포 APK의 서명이 다르면 업데이트 설치가 거부됩니다. 이 경우 기존 설정을 기록한 뒤 개발용 앱을 삭제하고 배포 APK를 설치해야 하며 앱 내부 설정/모델은 삭제될 수 있습니다. 이후 배포 APK끼리는 같은 배포 키로 업데이트합니다.

## 모델과 연결

Mac 앱에는 STT/LLM 모델 엔진을 포함하지 않습니다. 사용할 엔진/API를 별도로 준비하고 설정해야 합니다. 모바일 원격 모드는 Mac 앱을 켜 두고 동일한 내부 네트워크 또는 WireGuard로 연결합니다. 연결 키가 없는 서버이므로 외부 인터넷에 직접 공개하지 마세요.

Android 로컬 모드는 지원 모델을 먼저 다운로드해야 합니다. 모델별 라이선스와 저장 공간을 확인해 주세요. 시스템 음성 인식의 오프라인 지원은 기기/설정에 따라 다릅니다.

## 시험판의 범위

- 자동 업데이트는 비활성화되어 있습니다. 새 버전을 직접 내려받아 교체합니다.
- 장시간 안정성, 전체화면/다중 모니터, 장치 분리/잠자기 등의 조합은 검증 기록에 따라 제한적으로 지원합니다.
- 과거 컴퓨터 재부팅의 단일 원인은 확정되지 않았으며 완전 해결을 보장하지 않습니다.
- 문제가 생기면 OS/기기, 앱 버전, 로컬/원격 모드, 재현 순서를 알려주세요. 녹음 내용·API 키 등 개인 정보는 제외해 주세요.

원본 프로젝트의 MIT 라이선스를 유지합니다. 다운로드 파일의 `SHA256SUMS`, `LICENSE`, `THIRD_PARTY_NOTICES.txt`를 함께 제공합니다.
